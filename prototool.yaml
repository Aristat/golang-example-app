protoc:
  version: 3.8.0

generate:
  go_options:
    import_path: github.com/aristat/golang-example-app

  plugins:
    - name: go-grpc
      type: go
      flags: paths=source_relative
      output: generated
